// db-connector

var mysql = require('mysql');

var pool = mysql.createPool({
  connectionLimit : 10,
  host            : 'arthurktripp.com',
  user            : 'arthurktripp',
  password        : 'Covid19SUX',
  database        : 'central-oregon-services'
});

module.exports.pool = pool;